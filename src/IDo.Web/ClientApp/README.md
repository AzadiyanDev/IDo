# IDo Client

Angular client for the IDo web project.

## Local Development

```bash
npm install
npm start
```

## Build

```bash
npm run build
```

The production build writes browser assets to `../wwwroot/browser` for the .NET web host.
